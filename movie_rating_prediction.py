# -*- coding: utf-8 -*-
"""Movie Rating Prediction

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1b3gkCr41ay0vXs4JAGMwFz76haF3ynkD
"""

import pandas as pd

df = pd.read_csv("IMDb Movies India.csv")   # use your file name
df.head()

import pandas as pd

df = pd.read_csv("IMDb Movies India.csv", encoding='latin1')
df.head()

df.columns

df = df[['Genre','Director','Actor 1','Rating']]

df.dropna(inplace=True)

from sklearn.preprocessing import LabelEncoder

le = LabelEncoder()

df['Genre'] = le.fit_transform(df['Genre'])
df['Director'] = le.fit_transform(df['Director'])
df['Actor 1'] = le.fit_transform(df['Actor 1'])

X = df[['Genre','Director','Actor 1']]
y = df['Rating']

from sklearn.model_selection import train_test_split

X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2)

from sklearn.linear_model import LinearRegression

model = LinearRegression()
model.fit(X_train, y_train)

y_pred = model.predict(X_test)

from sklearn.metrics import mean_squared_error

print("MSE:", mean_squared_error(y_test, y_pred))

# Movie Rating Prediction - Task 2

import pandas as pd

# Load dataset (you already did this part)
df = pd.read_csv("IMDb Movies India.csv", encoding='latin1')

# Select required columns
df = df[['Genre','Director','Actor 1','Rating']]

# Remove missing values
df.dropna(inplace=True)

# Convert text to numbers
from sklearn.preprocessing import LabelEncoder
le = LabelEncoder()

df['Genre'] = le.fit_transform(df['Genre'])
df['Director'] = le.fit_transform(df['Director'])
df['Actor 1'] = le.fit_transform(df['Actor 1'])

# Define input and output
X = df[['Genre','Director','Actor 1']]
y = df['Rating']

# Split data
from sklearn.model_selection import train_test_split
X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2)

# Train model
from sklearn.linear_model import LinearRegression
model = LinearRegression()
model.fit(X_train, y_train)

# Predict
y_pred = model.predict(X_test)

# Evaluate
from sklearn.metrics import mean_squared_error
print("MSE:", mean_squared_error(y_test, y_pred))

